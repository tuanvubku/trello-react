API structure => http://localhost:3000/api/endpoint

############## API just for testing ########
=========SHOW ALL in db============
+show all user in db:
    endpoint: /users
    method: GET

+show all board in db:
    endpoint: /boards
    method: GET

+show all list in db:
    endpoint: /lists
    method: GET

+show all card in db:
    endpoint: /cards
    method: GET

+show all comment in db:
    endpoint: /comments
    method: GET

==============DELETE 1 BY NAME : xóa vậy test cho nhanh ===============   
+delete 1 user by name:
    endpoint: /users/delete/:username
    method: GET

+delete 1 board by name:
    endpoint: /boards/delete/:boardname
    method: GET

+delete 1 list by name:
    endpoint: /lists/delete/:listname
    method: GET

+delete 1 card by name:
    endpoint: /cards/delete/:cardname
    method: GET

============DELETE ALL in DB : xóa vậy test cho nhanh=================
+delete all user:
    endpoint: /users/delete-all
    method: GET

+delete all board:
    endpoint: /boards/delete-all
    method: GET

+delete all list:
    endpoint: /lists/delete-all
    method: GET

+delete all card:
    endpoint: /cards/delete-all
    method: GET

+delete all comment:
    endpoint: /comments/delete-all
    method: GET
==================================




############### API offical  ################

####### api user : 
+login:    
    endpoint: /users/login
    body: username, password
    method: POST

+register:    
    endpoint: /users/register
    body: username, password, role='user' || 'admin' , imageUrl 
    method: POST
    note: role & imageUrl : không bắt buộc, cái nào có thì bỏ vô, không thì đừng bỏ vô body 

+change password:    
    endpoint: /users/change-pass
    body: username, password, newPassword 
    method: POST

+update info user:     just for: role , imageUrl
    endpoint: /users/edit
    body: username, password, imageUrl, role='user' || 'admin'
    method: POST
    note: 
        nếu chỉ update role => đừng bỏ imageUrl vào body
	nếu chỉ update imageUrl=> đừng bỏ role vào body
        update both of them => push all to body


############### api board 
+ add new board
    endpoint: /boards/add
    body: name, ownerId, modeView: true | false , background, members 
    method: POST
    note: 
        modeView: true is public, false is privated
        members:  ['username1','username2']  hoặc list rỗng []
        background:  không bắt buộc, có thì bỏ vô, không thì đừng bỏ vô body 
     

+ delete board by _id :
    endpoint: /boards/delete
    body: _id
    method: POST

+edit board:   just edit : name, modeView, background 
    endpoint: /boards/edit
    body: _id, name, modeView, background 
    method: POST
    note: 
        field nào không thay đổi đừng bỏ vô body
        
+add members to board:
    endpoint: /boards/add-member
    body: _id, newMemberName 
    method : POST
    note: newMemberName là 1 username

+remove a members from board:
    endpoint: /boards/remove-member
    body: _id, memberName : one username
    method : POST
     note: memberName là 1 username

############# api list 
+add list :
    endpoint: /lists/add
    body: name,ownerId,boardId
    method : POST
+ edit list :  
    endpoint: /lists/edit
    body: _id, name, archived 
    method : POST
    note:
        field nào không thay đổi đừng bỏ vô body

+ delete list by _id
    endpoint: /lists/delete
    body: _id
    method : POST
   
################ api card 

+add card :
    endpoint: /cards/add
    body:title, ownerId, listId, deadline, description, label, members ,order,fileUrl
    method : POST
    note:
        deadline: string fomat 'dd-mm-yyyy'
        members: : ['username1','username2']
        label: string
        members có thể là rỗng [], 
	order không bắt buộc, default=0
        fileUrl : list String, ko bắt buộc
	field còn lại bắt buộc
	ko bắt buộc => nếu có thì bỏ vô body, ko thì đừng bỏ vô body

+ edit card :  
    endpoint: /cards/edit
    body: _id, title, deadline, description, label,order, archived, fileUrl
    method : POST
    note:
        field nào ko thay đổi => đừng bỏ vô body
        update name => only push name to body
        label: string    (blue, red ,...)
        archived :bool
        fileUrl: list string
        deadline: string fomat 'dd-mm-yyyy'
        ....
        
+add a members to card:
    endpoint: /cards/add-member
    body: _id, newMemberName 
    method : POST
    newMemberName : 1 username	

+remove a members from card:
    endpoint: /cards/remove-member
    body: _id, memberName 
    method : POST
    note: memberName : 1 username

+ delete card by _id
    endpoint: /cards/delete
    body: _id
    method : POST

+ move card to other list
    endpoint: /cards/move
    body: _id, newListId
    method : POST
     
 
############### api comment 

+add comment :
    endpoint: /comments/add
    body:content,ownerId,cardId,fileUrl 
    method : POST
    note:
       fileUrl ko bắt buộc, if not add => don't push to body

+ edit comment :  
    endpoint: /comments/edit
    body: _id, content, fileUrl
    method : POST
    note:
        field nào ko thay đổi  => don't push to body
        update content => chỉ bỏ content vô body
        ....
 
+ delete comment by _id
    endpoint: /comments/delete
    body: _id
    method : POST


#########   api query  ############

get all board of user by userid :
    endpoint: users/:_id/boards
    body: _id
    method : GET

get all list of board by boardid :
    endpoint: boards/:_id/lists
    body: _id
    method : GET

get all comment of card by cardid :
    endpoint: cards/:_id/comments
    body: _id
    method : GET
